El objetivo es ofrecer una experiencia de usuario óptima y consistente en cualquier dispositivo. Para lograrlo, se debe seguir una estrategia estricta y unificada, no basta con "ser responsive".

Principio #1: Mobile First Obligatorio 📱
Todo diseño y maquetación debe comenzar en la resolución móvil más pequeña soportada (ej. 360px). La adaptación a pantallas más grandes se realiza añadiendo estilos progresivamente mediante media queries. Esta es una regla obligatoria, ya que nos obliga a:

Priorizar el Contenido: Mostrar solo lo esencial en pantallas pequeñas.

Optimizar el Rendimiento: Cargar primero los recursos y estilos más ligeros.

Escribir CSS Más Limpio: Se evita tener que sobrescribir estilos constantemente.

En la práctica, esto significa que las barras de navegación complejas se convierten en un ícono de menú, los diseños de múltiples columnas se apilan verticalmente, y la funcionalidad secundaria se oculta para no saturar la vista principal.

Principio #2: Layout Fluido y Controlado en Escritorio 🖥️
Para mantener una apariencia consistente en pantallas grandes, el contenido no se estirará indefinidamente.

Regla: Todo el contenido principal debe estar dentro de un contenedor global que limite su ancho máximo. Usaremos una clase .l-container para este propósito. Este contenedor tendrá un ancho porcentual para ser fluido, pero un max-width para mantener la legibilidad.

Ejemplo en CSS puro:

CSS

/* Ruta: /assets/styles/layout/_container.css */

.l-container {
  width: 90%; /* Ocupa el 90% del ancho disponible */
  max-width: 1280px; /* Pero nunca será más ancho de 1280px */
  margin-left: auto; /* Centra el contenedor horizontalmente */ 
  margin-right: auto; /* Centra el contenedor horizontalmente */ 
}
Con esta regla, la web se adapta a cualquier tamaño de pantalla sin que el contenido se vea desproporcionado.

Principio #3: Puntos de Quiebre (Breakpoints) Unificados
Para asegurar la consistencia, todos los desarrolladores deben usar el mismo conjunto de puntos de quiebre.

Regla: Los breakpoints se definirán como Variables CSS (Custom Properties) en el selector :root para que estén disponibles globalmente y se usen en todos los media queries.

Definición de Variables (en un archivo global de CSS):

CSS

/* Ruta: /assets/styles/base/_variables.css */

:root {
  --breakpoint-sm: 576px;  /* Para tablets pequeñas en vertical */
  --breakpoint-md: 768px;  /* Para tablets en horizontal */
  --breakpoint-lg: 992px;  /* Para laptops pequeñas */
  --breakpoint-xl: 1200px;  /* Para pantallas de escritorio */
}
Uso en un Componente:
Se utilizan las variables con la función var() dentro de los media queries.

CSS

/* Ejemplo de uso en un componente de tarjeta */
.c-card {
  /* Estilos base (Mobile First) */
  width: 100%;
}

/* Cuando la pantalla sea 768px o más ancha... */
@media (min-width: var(--breakpoint-md)) {
  .c-card {
    width: 50%; /* La tarjeta ocupará la mitad del espacio */
  }
}

/* Cuando la pantalla sea 992px o más ancha... */
@media (min-width: var(--breakpoint-lg)) {
  .c-card {
    width: 33.33%; /* Ocupará un tercio */ 
  }
}
Esta estrategia asegura que el diseño se adapte de una manera predecible, controlada y fácil de mantener.







