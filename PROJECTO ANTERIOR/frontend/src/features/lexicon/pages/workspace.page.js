/* ==========================================================================
   Archivo: frontend/src/features/lexicon/pages/workspace.page.js
   Propósito: Página principal que ensambla los componentes de la aplicación.
   ========================================================================== */

const WorkspacePage = (() => {
  function init() {
    console.log('WorkspacePage inicializada. Ensamblando componentes...');
    // En una fase futura, este archivo podría ser responsable de renderizar
    // el layout principal y montar los componentes en el DOM.
    // Por ahora, su existencia es estructural.
  }

  return {
    init,
  };
})();
